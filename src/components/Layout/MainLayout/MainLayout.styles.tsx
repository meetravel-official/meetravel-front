import { css } from "@emotion/react";

import { COLORS } from "@/styles/color";

export const cssMainLayoutStyle = css`
  background: ${COLORS.WHITE};
  width: 540px;
  height: 100vh;
  margin: 0 auto;
`;
