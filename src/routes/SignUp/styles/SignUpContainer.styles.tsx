import { css } from "@emotion/react";

export const cssSignUpContainerStyle = css`
  min-height: inherit;
  position: relative;
  .button-to-next {
    position: absolute;
    left: 0;
    bottom: 0;
    width: 100%;
  }
  > div:nth-of-type(1) {
    padding: 0;
  }
`;
